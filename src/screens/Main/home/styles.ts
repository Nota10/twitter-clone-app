import { StyleSheet } from 'react-native';

export const homeStyles = StyleSheet.create({
  tabNavigator: {
    height: 70,
    padding: 0,
    paddingTop: 10,
    // backgroundColor: colors.secondaryDark,
    margin: 0,
    // borderTopColor: colors.secondaryDarkest,
  },
});
