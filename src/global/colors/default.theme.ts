import { DefaultColors } from '../../@types/colors';

const defaultColors: DefaultColors = {
  primary: { main: '#ffd500', dark: '#ffc000', darkest: '#ff9500' },
  common: {
    white: '#fff',
    black: '#000',
    blue: '#1DA1F2',
  },
};

export { defaultColors };
